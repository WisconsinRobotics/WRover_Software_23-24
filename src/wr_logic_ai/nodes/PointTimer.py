class PointTimer:

  def calculate_time(distance):
    #Sooooo, As far as I remember, we were gonna get the distance from list of dictionaries. are we going to reference that
    #when calling this function in the parameter distance or do i do that inside the function.
    speed = 0.5
    time = distance/speed
    return time
